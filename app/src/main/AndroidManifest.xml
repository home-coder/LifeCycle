<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jiangxiujie.lifecycle">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LifeCycleActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DiagActivity"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity android:name=".NormalActivity" />
        <activity android:name=".TestLinearLayout" />
        <activity android:name=".TestRelativeLayout" />
        <activity android:name=".TestFrameLayout" />
        <activity android:name=".PercentFrameLayout" />
        <activity android:name=".CustomUI"></activity>
    </application>

</manifest>
